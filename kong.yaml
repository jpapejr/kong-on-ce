 _format_version: "3.0"
 _transform: true

 services:
 - host: mockbin.org
   name: example_service
   port: 80
   protocol: http
   routes:
   - name: example_route
     paths:
     - /mock
     strip_path: true
 - host: www.google.com
   name: another_service
   port: 80
   protocol: http
   routes:
   - name: another_service_route
     paths:
     - /ggl
     - /google
     strip_path: true

